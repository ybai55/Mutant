#!/usr/bin/env bash

export VERSION=`python -m setuptools_scm`

if [[ -n `git status --porcelain` ]]; then
    VERSION=$VERSION-dirty
fi

docker build . --target mutant_server -t ghcr.io/Mutant/mutant-server:`bin/version`